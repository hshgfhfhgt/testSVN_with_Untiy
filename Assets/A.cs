using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class A : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("A 스크립트 내용을 변경했어요.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
